import {styled} from "../../../theme/theme";

export const Title = styled('h1', {
    fontWeight: 'lighter',
    margin: 0,
});

export const Text = styled('h3', {
    fontWeight: 'lighter',
    margin: 0,
});
