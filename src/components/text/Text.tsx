import {styled} from "../../../styles/stitches.config";

export const Title = styled('h1', {
    fontWeight: 'lighter',
    margin: 0,
});

export const Text = styled('p', {
    fontSize: '1.17em',
    fontWeight: 'lighter',
    margin: 0,
});
